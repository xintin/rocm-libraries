variables:
- group: common
- template: /.azuredevops/variables-global.yml@pipelines_repo

parameters:
- name: pipelinesRepoRef
  type: string
  default: refs/heads/develop
- name: triggerDownstreamJobs
  type: boolean
  default: true

resources:
  repositories:
  - repository: pipelines_repo
    type: github
    endpoint: ROCm
    name: ROCm/ROCm
    ref: ${{ parameters.pipelinesRepoRef }}

trigger:
  batch: true
  branches:
    include:
    - develop
    - release-staging/rocm-rel-7.*
  paths:
    include:
    - projects/hipsparselt
    exclude:
    - projects/hiptensor/.githooks
    - projects/hiptensor/.github
    - projects/hiptensor/.jenkins
    - projects/hiptensor/docs
    - projects/hiptensor/.*.y*ml
    - projects/hiptensor/LICENSE
    - projects/hiptensor/*.md

pr: none

stages:
- stage: hiptensor
  jobs:
  - template: ${{ variables.CI_COMPONENT_PATH }}/hipTensor.yml@pipelines_repo
    parameters:
      sparseCheckoutDir: projects/hiptensor
      triggerDownstreamJobs: ${{ parameters.triggerDownstreamJobs }}
- template: templates/report-summary-check-wrapper.yml